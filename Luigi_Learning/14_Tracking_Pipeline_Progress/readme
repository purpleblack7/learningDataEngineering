This is used to track the progress of your pipeline. I've added a 5 second sleep timer to slow things down.

I've also added the "self.set_progress_percentage(100 * counter / len(input_files) )" to show the percentage of the task done
